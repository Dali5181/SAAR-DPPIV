# Core dependencies for the SAAR-DPPIV scoring pipeline: classification,
# auxiliary pIC50 regression, source-aware LambdaRank ranking, decision
# fusion, evaluation, and the Streamlit web app.
#
# This does NOT include torch/transformers (needed only to compute real
# ESM-2 embeddings) -- see requirements_full_ranking.txt for that.
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
xgboost>=2.0
lightgbm>=4.0
catboost>=1.2
streamlit>=1.30
python-docx>=1.1
openpyxl>=3.1
